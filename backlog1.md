EPIC 2. Зафиксировать Stage 1 data foundation
US2.1. Ввести явные сущности Group и Section

Что сделать

не тащить всё через одну абстрактную GroupUnit;

выделить логическую группу и физическую секцию;

связать их во времени.

Acceptance criteria

в модели есть отдельные сущности Group и Section либо эквивалентное явное разделение;

можно ответить на вопросы:

в какой группе была корова на дату;

в какой секции стояла на дату;

в каких секциях размещалась группа.

US2.2. Ввести исторические связи

Что сделать

реализовать:

CowGroupMembership

CowSectionStay

GroupSectionAllocation

Acceptance criteria

история не затирается при новом импорте;

можно выбрать дату и восстановить состояние размещения;

перемещение коровы и смена группы различаются как разные события/факты.

US2.3. Утвердить master ID и mapping

Что сделать

корова: master ID из AFI;

группа: внутренний ID + внешние коды источников;

секция: внутренний ID + внешний код при наличии;

правила сопоставления DTM к группам/секциям.

Acceptance criteria

каждая сущность имеет один внутренний ID;

внешние идентификаторы хранятся отдельно;

импорт не создаёт дубли при повторной загрузке.

EPIC 3. Довести загрузку AFI до нормального ingestion
US3.1. Сохранять AFI JSON в БД

Что сделать

не читать AFI только на лету с диска;

сохранять коров, сигналы здоровья, воспроизводство, отёлы, охоту и прочие нужные факты.

Acceptance criteria

после импорта AFI в БД появляются:

коровы,

внешние ID,

AFI-события/наблюдения,

связи с группами, если они есть в данных;

повторный импорт идемпотентен.

US3.2. Логировать batch и ошибки AFI

Что сделать

завести batch history;

писать количество записей, ошибки, предупреждения.

Acceptance criteria

каждая загрузка AFI отражается в IntegrationBatch;

видно статус completed/error/partial;

по ошибке можно понять, какой файл упал и почему.

US3.3. Нормализовать 9 типов AFI-отчётов

Что сделать

привести 9 типов JSON к стабильной внутренней модели;

определить, что идёт в события, что в наблюдения, что в статусные флаги.

Acceptance criteria

по каждому типу отчёта есть mapping;

минимум 95% записей парсятся без ручного вмешательства на тестовом наборе;

некорректные записи не ломают весь import batch.

EPIC 4. Довести загрузку AIC / доений
US4.1. Импортировать AIC в MilkRecord

Что сделать

загрузка .AIC в таблицу доений;

связь с коровой по AFI ID/номеру;

дедупликация.

Acceptance criteria

после импорта AIC есть записи в MilkRecord;

одна и та же запись не дублируется при повторной загрузке;

есть связь с коровой либо пометка о невозможности матчинга.

US4.2. Нормализовать дату, сессию и качество записи

Что сделать

определить business date;

определить session;

нормализовать формат времени.

Acceptance criteria

записи доений можно корректно агрегировать по дню и сессии;

нет смешения временных зон/форматов;

тест на 30 днях даёт устойчивые итоги по суткам.

EPIC 5. Довести DTM ingestion до рабочего уровня
US5.1. Импортировать 4 Excel-отчёта DTM стабильно

Что сделать

потребление корма;

эффективность загонов;

история замесов;

остальные утверждённые отчёты первого контура.

Acceptance criteria

все утверждённые DTM-файлы импортируются скриптом;

по каждому файлу есть mapping колонок;

ошибки по одной строке не валят весь файл.

US5.2. Привязать DTM group-level данные к Group/Section

Что сделать

сопоставить groupCode/groupName в DTM к внутренним группам;

где нужно, связать с секцией через размещение группы.

Acceptance criteria

feed records отображаются на карточке группы;

если известна секция на дату, данные доступны и в секции;

несопоставленные записи отдельно помечаются как requires_mapping.

US5.3. Вынести mapping DTM в отдельную настраиваемую логику

Что сделать

не держать всё на хардкодах по заголовкам и имени файла;

сделать слой соответствия источника.

Acceptance criteria

mapping не размазан по разным скриптам;

новый формат файла можно добавить без переписывания половины системы;

есть журнал колонок/полей, которые не были распознаны.

EPIC 6. Реализовать основную пользовательскую навигацию Stage 1
US6.1. Сделать экран “Карта местности”

Что сделать

отображение зданий и секций;

показ по времени, кто находился в секции;

показ кормления по секции;

показ связанных событий.

Acceptance criteria

пользователь видит список/схему секций;

при выборе даты видно состав секции;

из секции можно перейти в группу и дальше к корове;

на секции отображаются связанные события и кормление.

US6.2. Сделать карточку группы

Что сделать

список коров группы;

сортировки и фильтры;

KPI группы;

секции, в которых группа размещена;

переходы к животным и событиям.

Acceptance criteria

карточка открывается из дашборда, карты и поиска;

список животных сортируется минимум по номеру, лактации, статусу, продуктивности;

видно секции размещения группы;

доступны переходы к карточке животного и событиям группы.

US6.3. Сделать карточку животного

Что сделать

паспортные данные;

продуктивность;

история событий;

ручные поля: вес, бонитировочная оценка;

ручной ввод допустимых параметров.

Acceptance criteria

карточка открывается из группы, поиска и событий;

есть базовый паспорт: ID, номер, группа, секция, лактация, статус;

есть история доений/сигналов/событий;

ручные поля сохраняются и отображаются в истории изменений.

EPIC 7. Довести dashboard до рабочего baseline
US7.1. Перевести dashboard на БД как основной источник

Что сделать

dashboard должен строиться из БД;

прямое чтение файлов оставить максимум как временный debug fallback.

Acceptance criteria

все KPI дашборда формируются из БД;

отключение папки с исходными файлами не ломает уже загруженный контур;

в UI видна дата/время последнего обновления.

US7.2. Утвердить и реализовать KPI первого этапа

Что сделать

надой;

молоко на голову;

тревоги здоровья;

репродуктивные сигналы;

кормление/затраты там, где уже есть данные;

свежесть загрузок.

Acceptance criteria

список KPI утверждён и не меняется “по вдохновению фронтендера”;

у каждого KPI есть источник и формула;

по клику можно провалиться в деталь.

US7.3. Сделать глобальные фильтры

Что сделать

период;

группа;

секция;

статус/домен там, где нужно.

Acceptance criteria

фильтры единообразны на основных экранах;

контекст сохраняется при переходах;

deep-link с параметрами открывает тот же контекст.

EPIC 8. Реализовать баланс молока
US8.1. Спроектировать модель баланса молока

Что сделать

валовый надой;

товарное молоко;

выпойка телятам;

потери/утилизация;

продажи;

выручка;

ручные корректировки.

Acceptance criteria

сущности и формулы согласованы;

у каждого потока есть источник или пометка manual;

“серые зоны” считаются явно, а не прячутся.

US8.2. Сделать экран баланса молока

Что сделать

верхний обзор потоков;

детализация по периоду;

журнал корректировок и расхождений.

Acceptance criteria

экран показывает структуру потоков молока;

пользователь может увидеть, куда делось молоко;

есть drill-down в группы доения, телят, продажи и корректировки.

US8.3. Добавить связь с экономикой

Что сделать

молоко → продажа → выручка;

кормление → стоимость;

возможность первого уровня маржинальности.

Acceptance criteria

для утверждённых данных видны базовые экономические связки;

отсутствующие данные помечены явно, а не подделываются нулями;

расчёты документированы.

EPIC 9. Довести раздел кормления из “заглушки” в рабочий экран
US9.1. Подключить feeding UI к реальным API и БД

Что сделать

использовать реальные данные DTM;

показать рационы, замесы, ингредиенты, факт/план.

Acceptance criteria

экран кормления не является статическим;

карточки и графики строятся по данным из БД;

ошибки загрузки DTM видны в интерфейсе.

US9.2. Определить основной объект кормления

Что сделать

явно решить: кормление считается по секции, по группе или в двух разрезах;

отразить это в интерфейсе.

Acceptance criteria

в UI нет путаницы “что именно мы сейчас смотрим”;

из рациона можно перейти в секцию и/или группу;

связь с затратами работает в том же разрезе.

US9.3. Сделать рабочие визуализации кормления

Что сделать

план/факт;

остатки;

dry matter;

IOFC;

cost per head;

история замесов.

Acceptance criteria

минимум 4 ключевых блока кормления работают на реальных данных;

есть фильтрация по периоду и группе/секции;

есть drill-down в замес и ингредиенты.

EPIC 10. Сделать настоящий event layer
US10.1. Перевести события в персистентную модель

Что сделать

не собирать события только на фронте;

хранить события как объекты системы.

Acceptance criteria

события пишутся в Event и типизируются через EventType;

есть связь с коровой, группой, секцией или системой;

лента событий строится с backend/data layer.

US10.2. Утвердить минимальный каталог событий

Что сделать

отёл;

осеменение;

перемещение;

тревога по здоровью;

отклонение по кормлению;

корректировка молока;

конфликт данных.

Acceptance criteria

каталог типов событий описан;

каждое событие имеет severity, source, timestamp;

события можно фильтровать по домену и объекту.

US10.3. Сделать карточку события

Что сделать

отдельный экран/панель с деталями;

связанный объект;

источник;

подтверждение/комментарий, если нужно.

Acceptance criteria

из ленты можно открыть событие;

видно, к чему оно относится;

ручные и импортные события отличимы визуально.

EPIC 11. Интеграции и качество данных
US11.1. Сделать рабочий экран “Интеграции”

Что сделать

список источников;

последние batch’и;

ошибки;

объём загрузки;

freshness.

Acceptance criteria

экран показывает реальное состояние интеграций;

по каждому источнику видны последние загрузки;

ошибочные batch’и не теряются.

US11.2. Ввести quality flags

Что сделать

missing

conflict

manual

stale

estimated

Acceptance criteria

ключевые записи и KPI могут быть помечены качеством;

UI умеет отображать эти статусы;

конфликтные записи доступны для анализа.

US11.3. Реализовать повторный импорт

Что сделать

ручной повторный прогон файла/пакета;

защита от дублей;

лог результата.

Acceptance criteria

повторный импорт доступен из admin/integrations;

успешный повторный импорт не создаёт дубли;

действия логируются.

EPIC 12. Поиск, отчёты, экспорт
US12.1. Сделать глобальный поиск

Что сделать

поиск по корове;

поиск по группе;

поиск по секции;

поиск по событию.

Acceptance criteria

поиск доступен из header;

результат ведёт в реальный объект;

поиск устойчив к номеру, части имени и внешнему ID там, где это допустимо.

US12.2. Сделать 2-3 отчёта первого этапа

Что сделать

молоко;

кормление;

здоровье/воспроизводство.

Acceptance criteria

отчёты формируются по реальным данным;

отчёты имеют фильтры по периоду и объекту;

формат стабилен и пригоден для обсуждения с заказчиком.

US12.3. Добавить экспорт

Что сделать

CSV/XLSX, а PDF где реально нужно.

Acceptance criteria

минимум 2 основных отчёта экспортируются;

экспорт уважает текущие фильтры;

выгрузка не ломает кодировку и числа.

EPIC 13. Минимальный access model для этапа 1
US13.1. Не строить фальшивый RBAC

Что сделать

на этапе 1 оставить одну роль admin/operator;

либо вообще без деления, но честно.

Acceptance criteria

в системе нет декоративных ролей без реальной логики;

ограничения, если есть, работают реально;

в документе зафиксировано, что полноценный RBAC отложен.

US13.2. Ограничить только опасные действия

Что сделать

ручная корректировка;

повторный импорт;

системные настройки.

Acceptance criteria

опасные действия не доступны анонимно;

есть хотя бы базовая защита/проверка;

аудит действий сохраняется.

EPIC 14. Приёмка этапа 1
US14.1. Подготовить test dataset за 30 дней

Что сделать

прогнать AFI, DTM, AIC;

проверить mapping;

сверить ключевые KPI.

Acceptance criteria

есть тестовый контур на 30 дней;

загрузка повторяема;

известные расхождения зафиксированы списком.

US14.2. Составить чек-лист приёмки

Что сделать

список обязательных экранов;

список обязательных переходов;

список обязательных метрик;

список обязательных интеграций.

Acceptance criteria

этап можно принять по формальным пунктам;

спорные места вынесены отдельно;

чек-лист согласован с командой.

US14.3. Провести demo/UAT этапа 1

Что сделать

показать сквозные сценарии:

секция → группа → корова;

рацион → секция/группа;

молоко → баланс → корректировки;

интеграция → batch → ошибка/успех.

Acceptance criteria

все обязательные сценарии проходят без ручного вмешательства в БД;

найденные баги заведены и классифицированы;

есть решение: stage 1 accepted / accepted with fixes / not accepted.

Порядок выполнения по приоритету
P0. Без этого этап 1 не закрыть

Собрать нормальный repo

Загрузить AFI в БД

Загрузить AIC в БД

Разделить Group / Section

Реализовать карту секций

Реализовать карточку группы

Реализовать карточку животного

Реализовать баланс молока

Реализовать event layer

Сделать экран интеграций

P1. Должно войти в этап 1

Довести feeding UI

Глобальные фильтры

Поиск

2-3 отчёта

Экспорт

Quality flags

P2. Можно добивать в хвосте этапа 1 или начале этапа 2

Улучшение экономики

Красивые диаграммы и polish

Частичный access control beyond single-role

Расширенные ручные формы

Definition of Done для Stage 1

Этап 1 считается закрытым, если:

проект разворачивается из одного исходного репозитория;

данные AFI, DTM, AIC загружаются в БД;

есть сущности коровы, группы, секции и их история;

работают экраны:

dashboard

map/sections

group

cow

feeding

milking/balance

events

integrations

работают переходы:

секция → группа → корова

рацион → секция/группа

молоко → группа/телята/выручка

есть минимум 2-3 отчёта;

30-дневный контур проходит UAT;

все критичные расхождения либо закрыты, либо явно зафиксированы.

Если хотите, следующим сообщением я превращу это в таблицу для Jira/ClickUp/Notion: Epic | Story | Priority | Estimate | Dependency | Acceptance Criteria.