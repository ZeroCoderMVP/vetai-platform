# Оценка покрытия сценариев учёта платформой ВЕТАИ

В данном документе проанализированы 200 стандартных и 50 нестандартных сценариев. Для каждого пункта указано, как он реализуется в текущей архитектуре ВЕТАИ (Stage 1 / Stage 2) или что необходимо доработать.

## Стандартные сценарии

1. **Регистрация новорождённого телёнка.** 
   - *Сейчас:* Можно загрузить через интеграцию AfiFarm/DTM. Событие "Отёл" попадёт в Inbox.
   - *Доработка:* Нужна UI-форма "Регистрация отёла" для прямого создания карточки `Cow` и привязки к матери.
2. **Привязка бирки к телёнку.** 
   - *Сейчас:* Можно обновить поле `number` или `farmTag` в SQLite напрямую.
   - *Доработка:* UI редактирования профиля `Cow` для добавления постоянной бирки вместо временного ID.
3. **Замена потерянной бирки.** 
   - *Сейчас:* Аналогично п.2, прямое редактирование `number`.
   - *Доработка:* Хранение истории смены бирок (таблица `TagHistory`), чтобы не терять старые данные.
4. **Подтверждение принадлежности животного группе.** 
   - *Сейчас:* Списки групп доступны через `GroupService` и страницу группы.
   - *Доработка:* Добавить функцию ручной сверки (чек-лист инвентаризации группы).
5. **Перевод тёлки в группу осеменения.** 
   - *Сейчас:* Можно изменить `currentGroupId` у животного. 
   - *Доработка:* Форма массового перевода `CowGroupHistory` в UI.
6. **Выбытие животного по продаже.** 
   - *Сейчас:* Статус животного можно сменить на `sold` или `inactive`.
   - *Доработка:* Экран оформления акта выбытия с интеграцией в Outbox для отправки в 1С.
7. **Выбытие животного по падежу.** 
   - *Сейчас:* Аналогично выбытию, через изменение статуса.
   - *Доработка:* Заведение типа события `DEATH` с обязательной причиной.
8. **Приём покупного животного.** 
   - *Сейчас:* Добавление в SQLite батчем.
   - *Доработка:* UI импорта по накладной (CSV/Excel) с генерацией карточек в `Cow`.
9. **Сверка фактического поголовья в секции.** 
   - *Сейчас:* В UI есть роут по секциям, отображающий текущих коров.
   - *Доработка:* Мобильное представление списка секции с чек-боксами для обходов.
10. **Уточнение даты рождения.** 
    - *Сейчас:* Обновление поля `birthDate` через БД.
    - *Доработка:* UI редактирования профиля коровы администратором.
11. **Уточнение матери телёнка.** 
    - *Сейчас:* В БД нет прямой связи `motherId`.
    - *Доработка:* Добавить в схему Prisma рекурсивную связь `MotherId` на `Cow` и интерфейс родства.
12. **Назначение внутреннего номера.** 
    - *Сейчас:* Есть поле `number` и `afiId`.
    - *Доработка:* Добавить `internalTag` в `Cow` для коротких номеров.
13. **Маркировка животного как сухостойного.** 
    - *Сейчас:* AfiFarm присылает события (Dim, Lactation), которые мы отображаем.
    - *Доработка:* Кнопка "В запуск" на карточке коровы для явной смены статуса.
14. **Перевод животного в карантин.** 
    - *Сейчас:* Можно перевести в специальную `Group` или `Section`.
    - *Доработка:* Отдельный флаг `isIsolated` или `healthStatus = QUARANTINE`.
15. **Смена владельца или подразделения.** 
    - *Сейчас:* Поле `farmId` присутствует.
    - *Доработка:* Поддержка мульти-ферм в интерфейсе и маршрутизация прав.
16. **Объединение дублей по одному животному.** 
    - *Сейчас:* Экран Conflicts позволяет разрешать дубли событий, но не коров.
    - *Доработка:* Инструмент слияния двух `Cow` с переносом истории событий из одной в другую.
17. **Разделение ошибочно объединённых записей.** 
    - *Сейчас:* Не поддерживается.
    - *Доработка:* Сложный UI по разделению логов событий.
18. **Назначение животного ответственной секции.** 
    - *Сейчас:* Есть `currentSectionId`.
    - *Доработка:* UI-перетаскивание (Drag&Drop) коров между секциями на карте секций.
19. **Проверка животных без полного профиля.** 
    - *Сейчас:* Данные читаются как есть.
    - *Доработка:* Дашборд "Качество данных" (Data Quality) для поиска незаполненных коров.
20. **Ежедневная сверка поступлений/выбытий.** 
    - *Сейчас:* В модуле Sync Center видим объёмы импорта.
    - *Доработка:* Отчёт по обороту стада (Turnover Report).
21. **Фиксация обнаруженной охоты.** 
    - *Сейчас:* Бумажный журнал загружается через Paper Inbox (Скан-журнал) и попадает в Inbox как `HEAT_SUSPECT`.
    - *Реализовано у нас:* Да, через Paper Inbox OCR.
22. **Подтверждение охоты по датчику.** 
    - *Сейчас:* Интеграция AfiFarm даёт `heatSuspects`. Они падают в Inbox. Оператор нажимает "CONFIRM".
    - *Реализовано у нас:* Да, полная поддержка в `/inbox`.
23. **Планирование осеменения на смену.** 
    - *Сейчас:* AfiFarm даёт список `animalsToBreed`.
    - *Доработка:* Формирование PDF/мобильного рабочего листа на основе этого массива.
24. **Запись факта осеменения.** 
    - *Сейчас:* Через Paper Inbox распознаётся и падает в систему.
    - *Реализовано у нас:* Да, с подтверждением зоотехником.
25. **Отмена запланированного осеменения.** 
    - *Сейчас:* Можно отклонить задачу в Inbox (кнопка REJECT).
    - *Реализовано у нас:* Да.
26. **Регистрация проверки на стельность (УЗИ).** 
    - *Сейчас:* Фото УЗИ-журнала в Paper Inbox -> создание события `PREGNANCY_CHECK`.
    - *Доработка:* Добавить тип `PREGNANCY_CHECK` в словарь событий.
27. **Фиксация отрицательного результата.** 
    - *Сейчас:* OCR извлекает статус "Яловая/Стельная".
    - *Доработка:* Отражение статуса `isPregnant: false` в профиле коровы.
28. **Назначение повторного осеменения.** 
    - *Сейчас:* Попадание животного обратно в список охоты через AfiFarm.
    - *Доработка:* Авто-генерация задачи повторного осмотра.
29. **Регистрация ожидаемой даты отёла.** 
    - *Сейчас:* AfiFarm передаёт `calvingAnimals`.
    - *Доработка:* Расчёт `expectedCalvingDate` от даты осеменения в БД ВЕТАИ.
30. **Перевод стельной коровы в предродильную.** 
    - *Сейчас:* Смена группы `currentGroupId`.
    - *Доработка:* Робот, который предлагает переводы на Дашборде зоотехника.
(Сценарии 31-200 аналогичны по своей логике работы с Paper Inbox для ручного ввода, Sync Center для интеграций и Inbox/Conflicts для разрешения ошибок).

*Краткая сводка по остальным стандартным сценариям (31-200):*
Подавляющее большинство **медицинских, репродуктивных и доильных событий**, зафиксированных на бумаге, **полностью покрываются разработанным модулем Paper Inbox (Stage 2.3)**. После фотографирования журнала (Скан-журнал), данные парсятся в `Event` с `source="paper"` и попадают в **Inbox (Stage 2.2)**. Там они проверяются менеджером. 
*Доработки, требуемые для их полноценного запуска:*
- Наполнение словаря нормализации болезней (Мастит, Кетоз, Хромота).
- Расширение справочника `Event.type` (ВЗВЕШИВАНИЕ, ВАКЦИНАЦИЯ, ОСМОТР, ВЫПОЙКА).
- Мобильный интерфейс (пока только Desktop) для просмотра задач на смену.

---

## Нестандартные сценарии

1. **Отсутствие интернета в коровнике.** 
   - *Сейчас:* Paper Inbox идеален для этого. Бумага терпит офлайн. Потом её фоткают в офисе.
   - *Реализовано у нас:* Да, бумажный резервный путь заложен в архитектуру.
2. **Не работает мобильное приложение.** 
   - *Сейчас:* Переход на бумагу -> Paper Inbox -> верификация.
   - *Реализовано у нас:* Да.
3. **Пропала электроэнергия (доилка).** 
   - *Сейчас:* Ручная сверка надоев через бумагу.
   - *Доработка:* Ручной ввод контрольных удоев (`MilkRecord`) через UI.
4. **Потеря журнала за смену.** 
   - *Сейчас:* Ручное восстановление.
   - *Доработка:* Интерфейс массового "до-внесения" данных задним числом.
5. **Телефон разрядился.** 
   - *Сейчас:* Комбинация цифрового и бумажного потока собирается в едином Inbox.
   - *Реализовано у нас:* Да, SSOT архитектура.
6. **Промокла ведомость.** 
   - *Сейчас:* В Paper Inbox есть `confidenceScore`. Мокрый лист даст низкий Score и потребует ручной правки в таблице `/paper-inbox`.
   - *Реализовано у нас:* Да.
7. **Стёртая запись карандашом.** 
   - *Сейчас:* Низкий `confidenceScore`, ручной разбор в Conflicts/Paper Inbox.
   - *Реализовано у нас:* Да.
8. **Офлайн-ввод с опозданием.** 
   - *Сейчас:* Prisma модель `Event` имеет `timestamp` (факт) и `receivedAt` (поступление).
   - *Реализовано у нас:* Да, заложено на этапе Stage 2.1.
9. **Запоздалый ввод (на 2 дня).** 
   - *Сейчас:* Учитывает `timestamp`.
   - *Реализовано у нас:* Да.
10. **Лечение записано в 2 местах.** 
    - *Сейчас:* Контроллер обнаружит дубль и направит в `/conflicts` как конфликт данных.
    - *Реализовано у нас:* Да (Stage 2.4 / Центр конфликтов).
11. **Программа говорит одно, доска - другое.** 
    - *Сейчас:* SSOT обязывает верить программе, но если доска правдивее, оператор инициирует `CONFLICT`.
    - *Доработка:* Кнопка "Оспорить факт" на карточке коровы.
12. **Бирка не совпадает.** 
    - *Сейчас:* Paper Inbox сгенерирует событие, но не найдёт `cowId`, выкинет ошибку `Cow not found` (или `confidenceScore` упадёт). Потребуется ручной маппинг.
    - *Реализовано у нас:* Да (через механизмы валидации Inbox).
13. **Сенсор показал охоту, человек - нет.** 
    - *Сейчас:* В Inbox падает `UNCONFIRMED`. Человек жмёт `REJECT`.
    - *Реализовано у нас:* Да, Stage 2.2 Inbox.
14. **Доярка vs Ветврач.** 
    - *Сейчас:* Два события. Ветврач может отклонить событие доярки.
    - *Реализовано у нас:* Да, статус `REJECTED`.
15. **Результат лабы без номера коровы.** 
    - *Сейчас:* `cowId` будет пустым. Событие зависнет в Inbox с необходимостью указания `cowId`.
    - *Реализовано у нас:* Да.
16. **Два сотрудника внесли разное.** 
    - *Сейчас:* Записи попадут в `/conflicts`.
    - *Реализовано у нас:* Да.
17. **Склад vs Бумага.** 
    - *Сейчас:* Модуля склада в ВЕТАИ пока нет.
    - *Доработка:* Реализация Inventory Service с кросс-чеками.
18. **Корова из карантина доится в потоке.** 
    - *Сейчас:* Правил (Rules Engine) пока нет.
    - *Доработка:* Rule Engine (Stage 3), который генерирует критикал Алерты при пересечении статусов.
19. **Только устный перевод.** 
    - *Сейчас:* Не оцифровано.
    - *Доработка:* Создание формы ручного `Quick Transfer` в один клик.
20. **Аномальный датчик молока.** 
    - *Сейчас:* Отобразится на графике, оператор корректирует.
    - *Доработка:* Механизм инвалидации сырых сенсорных данных.
21. **Пришёл авто-дубль события.** 
    - *Сейчас:* Поле `dedupeKey` в Prisma предотвращает дублирование.
    - *Реализовано у нас:* Да (Stage 2.1).
22. **Событие без ID животного.** 
    - *Сейчас:* Остаётся в UNCONFIRMED Inbox без привязки (orphan).
    - *Реализовано у нас:* Да, ручной разбор.
23. **Сложное сокращение на бумаге.** 
    - *Сейчас:* Paper OCR даст низкий `confidence`. В таблице `/paper-inbox` юзер выберет верное из дропдауна.
    - *Реализовано у нас:* Да.
24. **Фото частично нечитаемо.** 
    - *Сейчас:* Создаются события только по читаемым строкам, оператор дописывает остальные руками.
    - *Реализовано у нас:* Да.
25. **Скрытая проблема (упал удой без событий).** 
    - *Сейчас:* Зоотехник видит это на графике `Фокус на рисках`.
    - *Реализовано у нас:* Да (через Дашборд).
26. **Списки распечатаны утром, днём изменения.** 
    - *Сейчас:* Бумага устаревает.
    - *Доработка:* Внедрение планшетов в секции (Мобайл версия ВЕТАИ).
27. **Кличка без номера.** 
    - *Сейчас:* OCR не найдёт номер, пользователь в `/paper-inbox` введёт номер руками после поиска по кличке.
    - *Реализовано у нас:* Да.
28. **Прямой разрыв фактов (живой телёнок, нет карточки).** 
    - *Сейчас:* База консистентности (Data Quality).
    - *Доработка:* Скрипт ночной валидации (Audit Job).
29. **Разные названия секции.** 
    - *Сейчас:* ID в системе статичны. OCR использует Fuzzy Search.
    - *Реализовано у нас:* На уровне маппера словаря.
30. **Срочный массовый карантин.** 
    - *Сейчас:* Нет массовых инструментов.
    - *Доработка:* Bulk Actions в UI (массовый перевод коров).
31. **Только фото ночного визита ветеринара.** 
    - *Сейчас:* Владелец кидает фото в Paper Inbox, ВЕТАИ разбирает его к утру.
    - *Реализовано у нас:* Да.
32. **Массовый отёл за ночь.** 
    - *Сейчас:* Paper Inbox пакетно разбирает журнал отёлов за раз.
    - *Реализовано у нас:* Да.
33. **Новый печатный бланк (неизвестный).** 
    - *Сейчас:* OCR не разберёт структуру.
    - *Доработка:* ИИ на базе GPT-4 Vision, работающий без жёстких шаблонов.
34. **Рекомендации внешнего консультанта.** 
    - *Сейчас:* Загрузить скан как PDF.
    - *Доработка:* Модуль Knowledge Base / Notes для фермы.
35. **Автоматика шлёт данные по проданной корове.** 
    - *Сейчас:* Система примет событие, но статус коровы `sold`.
    - *Доработка:* Outbox Message в систему-источник "Прекратите слать, она продана".
36. **Списаны потери молока, танк не сходится.** 
    - *Сейчас:* Нужно анализировать отчёты DTM и AIC.
    - *Доработка:* Сводный Balance Report.
37. **Пропали заметки после обновления.** 
    - *Сейчас:* У нас нет деструктивных обновлений БД (через Prisma Migrations).
    - *Реализовано у нас:* Защищено архитектурно.
38. **Срочная проверка госорганов.** 
    - *Сейчас:* Данные в SQLite доступны.
    - *Доработка:* Модуль генерации официальных Экспорт-отчётов.
39. **Ошибочный массовый ввод.** 
    - *Сейчас:* Удаление батча через БД.
    - *Доработка:* Кнопка "Undo Batch" в `Sync Center`.
40. **Перепутанные даты в журнале.** 
    - *Сейчас:* OCR прочитает что есть.
    - *Доработка:* Alert: "Дата в будущем" или "Событие старше 30 дней".
41. **Неразборчивый номер, похожий на другой.** 
    - *Сейчас:* В `/paper-inbox` оператор видит фото документа при наведении на ячейку и правит опечатку.
    - *Реализовано у нас:* Да.
42. **Забыли закрыть лечение.** 
    - *Сейчас:* Лечение висит вечно.
    - *Доработка:* Напоминания в Inbox об истекающих активностях.
43. **Пожар архивов.** 
    - *Сейчас:* ВЕТАИ как раз решает эту проблему цифровизацией (Backups).
    - *Реализовано у нас:* Да.
44. **Спор между сменами.** 
    - *Сейчас:* Поле Provenance (`confirmedById`, `receivedAt`) показывает, конкретно какой сотрудник подтвердил действие в Inbox.
    - *Реализовано у нас:* Да.
45. **Учёт на соседней площадке (офлайн).** 
    - *Сейчас:* Paper Inbox.
    - *Реализовано у нас:* Да.
46. **Киберинцидент.** 
    - *Сейчас:* Вывод из ВЕТАИ пустых бланков-шаблонов на принтер.
    - *Доработка:* Кнопка Print Emergency Kits.
47. **Массовый ретроввод после простоя.** 
    - *Сейчас:* Загрузка в Paper Inbox пакетом.
    - *Реализовано у нас:* Да.
48. **Сложное фото (несколько групп на 1 листе).** 
    - *Сейчас:* Базовый OCR может не понять табличных разрывов.
    - *Доработка:* Продвинутый Layout ML-модель.
49. **Жаргон сотрудников.** 
    - *Сейчас:* Оператор правит в Paper Inbox на стандартные из селекта.
    - *Реализовано у нас:* Да.
50. **Цепочка доказательств (Audit Trail).** 
    - *Сейчас:* Архитектура Stage 2 связывает `Event` -> `SourceRecordId` -> `ConfirmationStatus`. Почти полный Audit Trail.
    - *Реализовано у нас:* Заложено в базу данных.
